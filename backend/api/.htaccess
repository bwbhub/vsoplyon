# Protege les fichiers sensibles
<FilesMatch "^(config\.php|config\.sample\.php|bootstrap\.php)$">
    Require all denied
</FilesMatch>

# Force le charset UTF-8
AddDefaultCharset UTF-8

# Desactive le listing du repertoire
Options -Indexes
