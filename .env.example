# URL de base de l'API PHP (sans slash final)
# En prod, le front et l'API sont servis depuis la meme origine (OVH),
# donc on utilise un chemin relatif.
# Production : /api        (meme domaine vsop-lyon.fr)
# Dev local  : http://localhost:8000/api   (si on lance PHP en local)
VITE_API_URL=/api
